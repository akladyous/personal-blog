<% if @comment.errors.any? %>
	<%= turbo_stream.update "form_comment_#{dom_id(@commentable)}", partial: 'form', locals: { commentable: @commentable, comment: @comment } %>
<% else %>
	<%= turbo_stream.prepend 'user_comments', partial: 'comment', locals: { comment: @comment} %>
<% end %>
