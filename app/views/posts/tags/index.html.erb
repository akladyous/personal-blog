<%= turbo_frame_tag post_tags_path(@post).parameterize do %>
	<div class="tags d-flex flex-wrap py-2">
		<% @tags.each do |tag| %>
			<%= render tag %>
		<% end %>
	</div>
<% end %>
<%# link_to "Show this tag", tag %>
<%# link_to "New tag", new_tag_path %>
