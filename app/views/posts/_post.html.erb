<% if controller.action_name == 'index' %>
	<%= render 'post_preview', post: post %>
<% else %>
	<%= turbo_frame_tag dom_id post do %>
		<div id="<%= dom_id post %>">
			<h1><%= post.title %></h1>
			<%= post.content %>
		</div>
	<% end %>
<% end %>
